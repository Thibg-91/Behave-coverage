[behave]
# Paths searched for feature files
paths = features

# Show all steps output even on success
show_skipped = true
show_timings = true

# Color output (disable in CI environments if needed)
color = true

# Stop at first failure
stop = false

# Logging
log_capture = false
stdout_capture = false
stderr_capture = false
